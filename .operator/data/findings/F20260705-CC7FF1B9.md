---
id: F20260705-CC7FF1B9
kind: finding
title: engine/pipeline files breach the 200-line cap, including three primitives and run-stage.ts
status: pending
priority: 4
source: code-quality#FINDING-001
created_at: '2026-07-05T09:13:53Z'
---

**Severity**: medium
**Priority**: 4
**Files Affected**: 11

**Pattern**: File-size cap for `engine/pipeline/**` (≤200 lines, primitives ≤200, `run-stage.ts` ~150) per `intelligence/rules/typescript.md` §FORBIDDEN + §Primitives boundary and `context.md` §Global Rules. Counts below exclude blank and comment lines (logging is heavy but even discounted the code bodies stay well over 200).
**Domain**: engine/pipeline (run-stage, primitives/, composers/)

**Impact**: The 200-line cap is the v5 tripwire for "a primitive is leaking" — the exact duplicate-plumbing accumulation that killed v4. It is breached across ~11 files and is NOT CI-enforced: `eslint.operator.config.js` has no `max-lines` rule, so oversized files pass `npm run lint` silently and keep growing. Verified non-blank/non-comment line counts:
- `engine/pipeline/run-stage.ts` — 619 total / ~388 code, vs its own header target of "~150 lines" (`run-stage.ts:26`).
- `engine/pipeline/primitives/persist-output.ts` — 543 / ~313 (primitive, hard ≤200).
- `engine/pipeline/primitives/aop-applier.ts` — 368 / ~233 (primitive).
- `engine/pipeline/primitives/observe-status.ts` — 337 / ~216 (primitive).
- `engine/pipeline/composers/pr-feedback-supervisor-stage.ts` — 583 / ~447.
- `engine/pipeline/composers/aop-planner-stage.ts` — 561 / ~425.
- `engine/pipeline/composers/discovery-iteration-stage.ts` — 402 / ~325.
- `engine/pipeline/composers/weekly-metrics-stage.ts` — 380 / ~311.
- `engine/pipeline/composers/closed-pr-recovery.ts` — 376 / ~293.
- `engine/pipeline/composers/verifier-driven-creator-stage.ts` — 306 / ~227.
- `engine/pipeline/pr-lifecycle.ts` — 365 / ~267.

**Fix**: Extract cohesive sub-units so each pipeline file lands under the cap without stripping logs/JSDoc: pull `run-stage.ts` orchestration helpers and the composers' prompt-assembly / bot-comment formatting into their own primitives or `_shared` modules; split the three oversized primitives (`persist-output`, `aop-applier`, `observe-status`) along their internal seams (commit/persist vs KV-write; parse vs apply; fetch vs classify). Then add an ESLint `max-lines` rule (200 for `engine/pipeline/**`, 300 elsewhere, `skipComments`/`skipBlankLines`) so the invariant becomes a real gate rather than review discipline.

**Acceptance Criteria**:
- [ ] Every `engine/pipeline/**` non-test file is ≤200 code lines (logging/comments/JSDoc excluded); primitives remain one-file-per-primitive with ≥95% coverage.
- [ ] `run-stage.ts` composes primitives only and returns to its ~150-line target.
- [ ] An ESLint `max-lines` rule enforces the caps so future breaches fail `npm run lint`.
- [ ] Build passes after fix (`npm run typecheck && npm run lint && npm test`).

