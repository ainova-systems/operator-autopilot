---
id: T20260806-E1A6585E
kind: task
title: Rewrite the architecture-v5 layout tree and §1.2 against the real composers directory
status: pending
priority: 3
created_at: '2026-09-04T14:04:10Z'
parent_id: F20260806-9B0ADAA5
---

# Replace the `stage-logic/` subtree in the docs with the real `composers/` contents

## Problem

`docs/architecture-v5.md:5` states: "Every claim in this doc is the code's current
shape — drift between this doc and `engine/` is a bug." The doc then renders, at
`:45-58`, a `stage-logic/` subtree holding `supervisor.ts`, `finding-plan.ts`,
`task-execute.ts`, `research.ts`, `retrospective.ts`, `rejection-handler.ts`,
`errors.ts`, `_shared/scratch.ts` — **none of the six stage-named files exists**.
§1.2 at `:176` repeats the claim in prose, and its extraction table at `:182-191`
is keyed entirely on those dead file names. `composers` appears zero times anywhere
under `docs/`, so the doc has no description of the layer that actually ships.

Four more surfaces carry the same dead path or the dead `agents/workflow/stages.yaml`
path (verified present):

- `README.md:31` — layout tree says `run-stage.ts + primitives/ + stage-logic/`
- `.operator/context/project.md:76` — High Priority Areas lists `stage-logic/**`
- `docs/workflow.md:162` and `:618` — `agents/workflow/stages.yaml`
- `scripts/check-ts-prune.mjs:40` and `scripts/check-knip.mjs:76` — comments naming
  `stage-logic/supervisor.ts` and "stage-logic deletion" (comments only, no path filter)

## Solution

**Enumerate the directory at implementation time — do not transcribe this list blindly.**
As verified during planning, `engine/pipeline/composers/` currently holds:

- Stage composers wired from `engine/engine/stage-handlers.ts:30,35,41,46,51` —
  `aop-planner-stage.ts`, `verifier-driven-creator-stage.ts`,
  `pr-feedback-supervisor-stage.ts`, `discovery-iteration-stage.ts`,
  `weekly-metrics-stage.ts`
- `closed-pr-recovery.ts` — the sixth stage-level composer, invoked from
  `discovery-iteration-stage.ts:23` rather than from `stage-handlers.ts`
- aop-planner split modules: `aop-planner-after-agent.ts`, `aop-planner-build-pr.ts`,
  `aop-planner-build-run-input.ts`, `aop-planner-synthesize.ts`
- `errors.ts` — `StageLogicError` + codes
- `_shared/` — **6** modules: `aop-planner-deps.ts`, `aop-planner-scratch.ts`,
  `planner-pr-body.ts`, `pr-summary.ts`, `scratch.ts`, `thread-dispositions.ts`
  (the parent finding said 3; it was stale)

Then:

1. Replace the `stage-logic/` subtree at `docs/architecture-v5.md:45-58` with the
   real `composers/` contents.
2. Rewrite §1.2 (`:174-191`): retitle it for the composer layer, restate the
   dissolution plan against file names that exist, and drop or remap every row keyed
   on `supervisor.ts` / `finding-plan.ts` / `task-execute.ts` / `research.ts` /
   `retrospective.ts` / `rejection-handler.ts`. Note that `_shared/scratch.ts` and
   `errors.ts` still exist under `composers/` and their stated destinations
   (`primitives/scratch-store.ts`, `engine/infra/errors.ts`) are unchanged.
3. Reconcile the `:5` preamble — it describes the dissolution as outstanding; keep
   that framing only where it is still true of the composer layer.
4. Fix `README.md:31` to name `composers/` instead of `stage-logic/`.
5. Fix `.operator/context/project.md:76` to name `engine/pipeline/composers/**`.
6. Fix `docs/workflow.md:162` and `:618` to `engine/content/prompts/stages.yaml`.
7. Fix the stale comments at `scripts/check-ts-prune.mjs:40` and
   `scripts/check-knip.mjs:76` — comment text only, do not change either script's
   ignore-list entries or behaviour.

Prose in `CHANGELOG.md:94,123` describing historical releases stays as-is — it
documents what shipped at the time. Do not touch `eslint.operator.config.js` or
`intelligence/**`; those are separate tasks so each stays revertible in one commit.

## Affected Files

- `docs/architecture-v5.md:5` — preamble drift statement
- `docs/architecture-v5.md:45-58` — layout tree subtree
- `docs/architecture-v5.md:174-191` — §1.2 heading, prose, and extraction table
- `docs/workflow.md:162` — stages.yaml path
- `docs/workflow.md:618` — stages.yaml path
- `README.md:31` — monorepo layout line
- `.operator/context/project.md:76` — High Priority Areas
- `scripts/check-ts-prune.mjs:40` — stale comment
- `scripts/check-knip.mjs:76` — stale comment

## Acceptance Criteria

- `docs/architecture-v5.md` layout tree and §1.2 list the files actually present under
  `engine/pipeline/composers/`, with no reference to `supervisor.ts`, `finding-plan.ts`,
  `task-execute.ts`, `research.ts`, `retrospective.ts`, or `rejection-handler.ts`
- `grep -rn "stage-logic" docs .operator/context README.md scripts` returns no reference
  to `engine/pipeline/stage-logic/`
- `grep -rn "agents/workflow/stages.yaml" docs` returns nothing
- Every file the doc names under `engine/pipeline/composers/` resolves on disk
- LF line endings preserved on every touched file
- `npm run typecheck && npm run lint && npm test` all pass

