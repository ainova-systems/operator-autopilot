# Runtime state (SQLite DB + workspaces — configurable via OPERATOR_WORKSPACES_DIR / OPERATOR_DB_PATH)
/state/
/workspace/
/.workspace/
/.operator-data/

# Node
node_modules/
dist/
coverage/
*.tsbuildinfo

# Next.js
.next/
next-env.d.ts
out/

# ESLint cache
.eslintcache

# Environment
.env
.env.local
*.pem

# Instance config (local-only repo binding; the committed template is
# config/repos.yaml.example — manage repos at runtime through the app UI)
/config/repos.yaml

# Internal planning docs (migration plan, future phases, incident journal — not published)
/docs/temp/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# AI IDE tools (generated by intelligence/scripts/sync.sh, local preferences)
CLAUDE.md
CODEX.md
.cursorrules
.claude/
.cursor/
.agents/
.codex/
intelligence/_backup/
**/.tmp/
/.playwright-mcp/*
/*.png
.runtime-logs/
